export default async function LayoutUser({ children }) {
    // await fetch (user)
  
    // if(!user) => redirect to (component ou page SignUp) à voir
  
    // if (role) => you ok to see this pages
  
    return <div>{children}</div>;
  }